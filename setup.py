# setup.py
from setuptools import setup, find_packages

setup(
    name='bin',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Your Name',
    description='a libray for handling binary data',
)
